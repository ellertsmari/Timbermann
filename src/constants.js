const TILE_HEIGHT = 50;
const TILE_WIDTH = 40;
const HEIGHT = Math.floor(innerHeight/TILE_HEIGHT);
const WIDTH = Math.floor(innerWidth/TILE_WIDTH)-1;

export { TILE_HEIGHT, TILE_WIDTH, HEIGHT, WIDTH };